# v0.8.0 — Test-framework integration (preview)

[Index](roadmap.md) · [v0.7.0](v0.7.0-roadmap.md) · [v0.9.0](v0.9.0-roadmap.md)

**Status:** preview — deferred from v0.4.0 diagnostics draft.

## Theme

Optional xUnit helpers and CI-friendly failure output.

## Main outcome

- Optional `Braid.Xunit` package (not in core `braid`)
- `ITestOutputHelper` helpers for replay token and diagnostics blocks
- CI attachment patterns documented

Candidates from [v0.4.0-diagnostics.md](v0.4.0-diagnostics.md) (if revived):

- `BraidXunit.WriteFailure(...)`
- Optional `BraidFailureReport` DTO

## Must not do

- xUnit dependency in core package
- Forced test-framework coupling for library consumers

## v0.4.0 doc-only alternative (already shipped)

`output.WriteLine(ex.ToString())` and `TryGetReplayText` without a separate package.
