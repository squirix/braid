# v0.5.0 — Product packaging and featured examples

[Index](roadmap.md) · [v0.4.0](v0.4.0-roadmap.md) · [v0.6.0](v0.6.0-roadmap.md)

**Status:** planned (recommended next release).

## Goals

- README restructure for a clear product story
- Three featured examples with walkthroughs
- “When to use Braid” and “not Coyote replacement” guidance
- Package metadata / NuGet readme alignment
- **Stable API** — no `ExploreAsync` (reserved for v0.6.0)

## README structure (target)

1. Hero + install (.NET 10)
2. Quick start (`Hit` + replay token)
3. Replay token (short; link workflow)
4. When to use Braid / vs stress / vs Coyote
5. Examples (three featured)
6. Arrive / Release (collapsed; link v0.3 material)
7. Limitations and non-goals

## Three featured examples

| # | Example | Work |
|---|---------|------|
| 1 | **Lost update** | New `examples/lost-update/` + `docs/examples/lost-update.md`; from `RunAsyncReplaysScriptedScheduleThatReproducesLostUpdate` |
| 2 | **Cache / CAS** | Keep `examples/cache-cas-race`; README prominence |
| 3 | **Cancellation before observation** | New `examples/cancellation-before-observation/` — standalone; cancelled before observe must not count as observed |

`examples/user-operation-limiter` stays supplementary (limiter pattern), not a headline example.

## Scope

- README rewrite / structure
- Example projects + `docs/examples/` walkthroughs
- Comparison guidance (stress, Coyote, interception) in README or `docs/`
- Package description/tags if needed

## Out of scope

- `ExploreAsync` implementation
- New scheduler semantics (unless critical bugfix)
- `Braid.Xunit` package
- xUnit dependency in core library

## PRs

| PR | Scope |
|----|-------|
| `readme-product-positioning-v0.5` | README restructure, Coyote/stress guidance |
| `example-lost-update` | `examples/lost-update/` + walkthrough |
| `example-cancellation-before-observation` | Standalone example + docs |
| `examples-polish-v0.5` | Cache-CAS prominence, examples index |
| `release-v0.5.0` | Changelog + version bump |

## Exit criteria

- [ ] Three featured examples build and run under CI
- [ ] README: coherent story with replay token front and center
- [ ] Clear “when to use / when not” section
- [ ] API unchanged from v0.4.0 (no `ExploreAsync`)

## Test plan

- Example test projects in solution / CI
- Full `braid.tests` regression
- Manual: run one example, copy replay token into regression test

## Must not do

- `ExploreAsync`
- Scheduler semantics creep
- xUnit in core package
