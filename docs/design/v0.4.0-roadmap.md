# v0.4.0 — Replay token, diagnostics, runtime boundaries

[Index](roadmap.md) · [v0.5.0](v0.5.0-roadmap.md)

**Status:** shipped (see [CHANGELOG.md](../../CHANGELOG.md) **0.4.0**).

## Goals

- Replay-token extraction ergonomics (`TryGetReplayText` + docs)
- Diagnostics polish (copy-paste workflow)
- Runtime-boundary hardening (enforcement + documentation)
- Honest random-only failure limits
- **No** new scheduling semantics beyond documented invariants

## Shipped

| Item | Notes |
|------|-------|
| `BraidRunException.TryGetReplayText` | Non-throwing canonical replay text export |
| Overlapping probe rejection | `ProbeWaitInFlight`; flowing child concurrent hits fail clearly |
| [replay-token-workflow.md](../replay-token-workflow.md) | Workflow doc |
| [runtime-boundaries.md](../runtime-boundaries.md) | One probe wait per worker; child-task rules |
| README | Replay token section, tagline, `TryGetReplayText` |

## Out of scope (deferred)

- `Braid.Xunit` package → [v0.8.0](v0.8.0-roadmap.md)
- `BraidFailureReport` DTO
- `ExploreAsync` → [v0.6.0](v0.6.0-roadmap.md)
- New example projects → [v0.5.0](v0.5.0-roadmap.md)

## PRs (completed)

| PR | Scope |
|----|-------|
| `release-v0.4.0` | Changelog, version **0.4.0**, ship API + scheduler fix |
| `readme-replay-token-v0.4` | README replay token + `TryGetReplayText` |
| `docs-replay-token-workflow` | Workflow doc |
| `docs-runtime-boundaries-v0.4` | Boundary doc |

## Exit criteria

- [x] v0.4.0 in CHANGELOG; package version 0.4.0
- [x] Replay token documented (= canonical replay text)
- [x] Runtime boundaries documented and match tests
- [x] `dotnet test` green
- [x] Random-only failures do not synthesize full schedule

## Test plan

- Full `dotnet test`
- `BraidRunExceptionTryGetReplayTextTests`, `BraidRuntimeBoundaryTests`, `BraidFailureReportTests`

## Must not do

- xUnit package in core
- `ExploreAsync`
- Scheduler semantic changes (replay / random / Arrive–Hit–Release)
- IL rewriting, TaskScheduler replacement, await interception
