# Cancellation before observation

Shows how to replay a race where cancellation happens before an operation is observed.

Run from the repository root:

```bash
dotnet test examples/cancellation-before-observation/Braid.Examples.CancellationBeforeObservation.csproj
```

Walkthrough: [docs/examples/cancellation-before-observation.md](../../docs/examples/cancellation-before-observation.md).
